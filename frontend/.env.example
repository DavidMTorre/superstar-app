# URL base del backend Laravel (opcional).
# Si está vacío, en desarrollo las rutas /api se proxifican a http://127.0.0.1:8000 (ver vite.config.js).
# Ejemplo producción:
# VITE_API_URL=https://api.tudominio.com

VITE_API_URL=
